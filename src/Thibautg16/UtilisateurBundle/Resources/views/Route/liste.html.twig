{# Thibautg16/groupeBundle/Resources/views/Route/liste.html.twig #}
 
{% extends "Thibautg16SqueletteBundle::layout.html.twig" %}
 
{% block title %}
	{{ parent() }} - Liste Routes
{% endblock %}

{% block fil_ariane %} 
	{{ parent() }} 
    <li><a href="#">Accueil</a></li>
    <li class="active"><a href="{{ path('thibautg16_route_liste') }}">Routes</a></li>
{% endblock %}

{% block body_thibautg16 %}    
    <div class="col-sm-12">
        <fieldset>       
		  <legend><h3>Liste des routes</h3></legend>
    	    <input id="filter" class="form-control" type="text" placeholder="Recherche">
    	    <br/>
		<table class="table footable" data-page-size="10" data-filter="#filter">
        	<thead>
                <tr>
                    <th data-toggle="true" data-type="numeric">#</th>
                    <th>Nom</th>
                    <th>Route</th>
                    <th>Modifier</th>
                    <th>Supprimer</th>
                </tr>
    		</thead>
            
        	<tbody>
                {% for route in listeRoute %}        
                <tr>
                    <td> {{ route.id }} </td>
                    <td> {% if route.nom is defined %} {{ route.nom }} {% endif %} </td>       
                    <td> {% if route.route is defined %} {{ route.route }} {% endif %} </td>
                    <td><a href="{{ path('thibautg16_route_modifier', {'idRoute':route.id}) }}"><span class="glyphicon glyphicon-pencil"></span></a></td>
                    <td><a href="{{ path('thibautg16_route_supprimer', {'idRoute':route.id}) }}"><span class="glyphicon glyphicon-remove"></span></a></td>
                </tr>
                {% endfor %}
    		</tbody>

            <tfoot class="hide-if-no-paging">
                    <tr>
                            <td colspan="9">
                                    <div class="pagination pagination-centered"></div>
                            </td>
                    </tr>
            </tfoot>
		</table>
        </fieldset>
	</div>
{% endblock %}